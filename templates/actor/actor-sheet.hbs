<form class="{{cssClass}}" autocomplete="off">
  
  {{!-- Sheet Header --}}
  <header class="sheet-header">
    <img class="profile-img" src="{{actor.img}}" data-edit="img" title="{{actor.name}}" />
    
    <div class="header-details">
      <h1 class="charname">
        <input name="name" type="text" value="{{actor.name}}" placeholder="Character Name" />
      </h1>
      
      <div class="sequence-box">
        <label>Sequence</label>
        <input type="number" name="system.sequence" value="{{system.sequence}}" min="0" max="9" />
        <a class="action-roll item-control" title="Roll Action"><i class="fas fa-dice-d20"></i></a>
      </div>
    </div>
  </header>

  {{!-- Madness Section --}}
  <section class="madness-section">
    <div class="madness-bar-container">
      <div class="madness-label">Madness ({{system.madness}}%)</div>
      <div class="madness-bar" title="{{system.madness}}%">
        <div class="madness-fill" style="width: {{system.madness}}%;"></div>
      </div>
      <input type="number" name="system.madness" value="{{system.madness}}" min="0" max="100" />
    </div>
  </section>

  {{!-- Anchor Section --}}
  <section class="anchor-section">
    <h2>Anchor</h2>
    <textarea name="system.anchor" placeholder="Describe the character's anchor to reality...">{{system.anchor}}</textarea>
  </section>

  {{!-- Inventory & Abilities Section --}}
  <section class="items-section">
    <h2>Abilities & Artifacts</h2>
    
    {{!-- Pathway Powers --}}
    <div class="item-list">
      <h3 class="list-header">Pathway Powers</h3>
      {{#each pathwayPowers as |item id|}}
      <div class="item" data-item-id="{{item._id}}">
        <div class="item-name">
          <img src="{{item.img}}" title="{{item.name}}" width="32" height="32" />
          <h4>{{item.name}}</h4>
        </div>
        <div class="item-controls">
          <a class="item-control item-edit" title="Edit Item"><i class="fas fa-edit"></i></a>
          <a class="item-control item-delete" title="Delete Item"><i class="fas fa-trash"></i></a>
        </div>
      </div>
      {{/each}}
      {{#unless pathwayPowers.length}}
      <div class="empty-list">No powers unlocked.</div>
      {{/unless}}
    </div>

    {{!-- Sealed Artifacts --}}
    <div class="item-list">
      <h3 class="list-header">Sealed Artifacts</h3>
      {{#each sealedArtifacts as |item id|}}
      <div class="item" data-item-id="{{item._id}}">
        <div class="item-name">
          <img src="{{item.img}}" title="{{item.name}}" width="32" height="32" />
          <h4>{{item.name}}</h4>
        </div>
        <div class="item-controls">
          <a class="item-control item-edit" title="Edit Item"><i class="fas fa-edit"></i></a>
          <a class="item-control item-delete" title="Delete Item"><i class="fas fa-trash"></i></a>
        </div>
      </div>
      {{/each}}
      {{#unless sealedArtifacts.length}}
      <div class="empty-list">No sealed artifacts possessed.</div>
      {{/unless}}
    </div>

    {{!-- Potion Formulas --}}
    <div class="item-list">
      <h3 class="list-header">Potion Formulas</h3>
      {{#each potionFormulas as |item id|}}
      <div class="item" data-item-id="{{item._id}}">
        <div class="item-name">
          <img src="{{item.img}}" title="{{item.name}}" width="32" height="32" />
          <h4>{{item.name}}</h4>
        </div>
        <div class="item-controls">
          <a class="item-control item-edit" title="Edit Item"><i class="fas fa-edit"></i></a>
          <a class="item-control item-delete" title="Delete Item"><i class="fas fa-trash"></i></a>
        </div>
      </div>
      {{/each}}
      {{#unless potionFormulas.length}}
      <div class="empty-list">No formulas acquired.</div>
      {{/unless}}
    </div>
  </section>

</form>
